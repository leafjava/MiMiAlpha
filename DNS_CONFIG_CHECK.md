# DNS 配置检查

## 📋 需要的 DNS 记录

根据你的截图，你需要配置以下记录：

### ✅ 必需记录

#### 1. API 服务器（指向阿里云 ECS）
```
类型: A
主机记录: api
记录值: 47.93.166.48
TTL: 600 或 10分钟
```

#### 2. 前端（指向 Vercel）
```
类型: CNAME
主机记录: mimialpha (或 app)
记录值: cname.vercel-dns.com
TTL: 600 或 10分钟
```

---

## 🔍 从截图看到的问题

从你的截图中，我看到：

1. ✅ 有一条 A 记录指向 `47.21.16.48`
2. ⚠️ 但 IP 地址似乎不对！

**你的阿里云 ECS IP 是多少？**

从之前的配置文件看到的 IP：
- `47.93.166.48` (DEPLOYMENT_SUMMARY.md)
- `172.28.109.217` (DEPLOYMENT_SUMMARY.md 内网 IP)

**请确认：**
- 如果是公网 IP，应该是 `47.93.166.48`
- 如果截图中的 `47.21.16.48` 是你的实际公网 IP，那就用这个

---

## ✅ 正确的配置应该是

### 方案 A：使用 47.93.166.48（之前文档中的 IP）

| 类型 | 主机记录 | 记录值 | TTL |
|------|---------|--------|-----|
| A | api | 47.93.166.48 | 600 |
| CNAME | mimialpha | cname.vercel-dns.com | 600 |

### 方案 B：使用 47.21.16.48（截图中的 IP）

| 类型 | 主机记录 | 记录值 | TTL |
|------|---------|--------|-----|
| A | api | 47.21.16.48 | 600 |
| CNAME | mimialpha | cname.vercel-dns.com | 600 |

---

## 🧪 如何确认正确的 IP

### 方法 1：SSH 登录服务器查看

```bash
# 登录服务器
ssh root@你的IP

# 查看公网 IP
curl ifconfig.me
# 或
curl ip.sb
```

### 方法 2：在宝塔面板查看

1. 登录宝塔面板
2. 首页会显示服务器的公网 IP

### 方法 3：在阿里云控制台查看

1. 登录阿里云控制台
2. 云服务器 ECS → 实例列表
3. 查看实例的公网 IP

---

## 📝 配置步骤

### 1. 删除错误的记录（如果有）

在你的域名管理界面，删除所有不需要的记录。

### 2. 添加正确的记录

#### 添加 A 记录（API）
```
类型: A
主机记录: api
记录值: [你的服务器公网IP]
TTL: 600
```

#### 添加 CNAME 记录（前端）
```
类型: CNAME
主机记录: mimialpha
记录值: cname.vercel-dns.com
TTL: 600
```

### 3. 等待 DNS 生效

通常需要 5-10 分钟，最多 24 小时。

---

## 🔍 测试 DNS 是否生效

### Windows 测试

```cmd
# 测试 API 域名
nslookup api.leaf666.icu

# 应该返回你的服务器 IP
# 例如: 47.93.166.48 或 47.21.16.48

# 测试前端域名
nslookup mimialpha.leaf666.icu

# 应该返回 Vercel 的 IP（通过 CNAME）
```

### 在线测试工具

访问以下网站测试：
- https://tool.chinaz.com/dns/
- https://www.whatsmydns.net/

输入：
- `api.leaf666.icu`
- `mimialpha.leaf666.icu`

---

## ⚠️ 常见错误

### 错误 1：主机记录填写错误

❌ 错误：填写完整域名 `api.leaf666.icu`
✅ 正确：只填写 `api`

❌ 错误：填写 `@`（这是根域名）
✅ 正确：填写 `api` 或 `mimialpha`

### 错误 2：CNAME 记录值错误

❌ 错误：`https://cname.vercel-dns.com`
✅ 正确：`cname.vercel-dns.com`（不要加 https://）

### 错误 3：IP 地址错误

❌ 错误：使用内网 IP（如 172.28.x.x, 192.168.x.x, 10.x.x.x）
✅ 正确：使用公网 IP（如 47.93.166.48）

---

## 📊 完整配置示例

假设你的服务器公网 IP 是 `47.93.166.48`：

```
记录类型    主机记录    记录值                    TTL
------------------------------------------------------
A          api        47.93.166.48             600
CNAME      mimialpha  cname.vercel-dns.com     600
```

配置完成后，你将可以访问：
- 前端：https://mimialpha.leaf666.icu
- API：https://api.leaf666.icu

---

## 🆘 需要帮助？

请告诉我：
1. 你的服务器公网 IP 是多少？（47.93.166.48 还是 47.21.16.48？）
2. 截图中的 DNS 记录是否已经保存？
3. 是否已经在宝塔面板添加了网站？

我可以帮你确认配置是否正确！
