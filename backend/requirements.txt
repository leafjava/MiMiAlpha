# ============================================
# MiMiAlpha Backend 完整依赖列表
# ============================================

# 核心 Web 框架
Flask==3.0.0
Flask-CORS==4.0.0

# HTTP 请求
requests==2.31.0

# 环境变量管理
python-dotenv==1.0.0

# OpenAI API（用于 AI 功能）
openai==0.28.1

# 生产环境服务器（可选，用于宝塔部署）
gunicorn==21.2.0

# 进程管理（可选，用于后台运行）
supervisor==4.2.5
