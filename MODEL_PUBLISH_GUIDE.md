# 模型发布功能使用指南

## 功能概述
模型发布功能允许量化交易者将自己的预测模型发布到平台，通过质押 TRX 来保证信号质量，并设置订阅价格进行变现。

## 功能特性

### 1. 表单字段
- **模型名称**：模型的显示名称（必填）
- **模型类型**：黄金/加密货币/股票/外汇（必选）
- **模型描述**：详细描述模型策略、适用场景、历史表现（必填）
- **单次信号价格**：用户购买单个信号的价格（TRX）
- **月度订阅价格**：用户订阅 30 天的价格（TRX）
- **质押金额**：最低 10 TRX，用于保证信号质量

### 2. 质押机制
- **最低质押**：10 TRX
- **质押用途**：保证信号质量，防止恶意发布
- **罚没规则**：
  - 信号准确率 < 70%：罚没 10%
  - 连续 3 次错误：自动退款订阅者 50%
  - 质押金不足：模型自动暂停

### 3. 链上业绩追溯
- 每笔信号预测和实际结果记录在 TRON 链上
- 形成不可篡改的业绩记录
- 机构可以验证历史表现

## 使用流程

### 步骤 1：连接钱包
1. 点击页面顶部的"连接钱包"按钮
2. 在 TronLink 中确认连接
3. 确保钱包在 Nile 测试网

### 步骤 2：切换到发布标签
1. 点击"📤 发布模型"标签
2. 查看当前钱包地址和 TRX 余额

### 步骤 3：填写模型信息
```
模型名称：黄金价格预测模型 V2
模型类型：🥇 黄金
模型描述：基于机器学习的黄金价格预测，夏普比率 2.5，历史准确率 82%
单次信号价格：1 TRX
月度订阅价格：10 TRX
质押金额：50 TRX
```

### 步骤 4：提交发布
1. 点击"发布模型并质押"按钮
2. 系统会验证：
   - 所有必填字段已填写
   - 质押金额 ≥ 10 TRX
   - 钱包余额充足
3. 在 TronLink 中确认质押交易
4. 等待交易确认

### 步骤 5：发布成功
- 显示交易哈希
- 表单自动重置
- 切换回浏览标签
- 模型将在审核后上线

## 表单验证规则

### 必填验证
- ❌ 模型名称不能为空
- ❌ 模型描述不能为空
- ❌ 单次信号价格必须 > 0
- ❌ 月度订阅价格必须 > 0
- ❌ 质押金额必须 ≥ 10 TRX

### 余额验证
- ❌ TRX 余额不足以支付质押金额
- ✅ 余额充足，可以发布

## 技术实现

### 当前实现
```typescript
// 1. 表单状态管理
const [publishForm, setPublishForm] = useState({
  name: '',
  type: 'gold',
  description: '',
  pricePerSignal: '',
  monthlySubscription: '',
  stakeAmount: '',
});

// 2. 质押交易
const txHash = await executePayment(
  currentAddress,
  contractAddress,
  publishForm.stakeAmount,
  `Stake for model: ${publishForm.name}`
);

// 3. 发布成功后重置表单
setPublishForm({ /* 初始值 */ });
setActiveTab('browse');
```

### 合约地址
- **Nile 测试网**：`TTn6Y1UwTbqQGXmwZJPqXNi1x5BpdqHtFN`
- 质押的 TRX 会发送到此合约地址

### 未来改进
目前使用简单的 TRX 转账模拟质押，未来需要：
1. 实现完整的模型注册合约函数
2. 添加模型元数据存储（IPFS）
3. 实现信号发布和验证逻辑
4. 添加质押金管理和罚没机制
5. 实现自动退款功能

## 测试场景

### 场景 1：正常发布
```
输入：
- 名称：测试模型
- 类型：黄金
- 描述：测试描述
- 单次价格：1 TRX
- 月度价格：10 TRX
- 质押：50 TRX

预期：
✅ 弹出 TronLink 交易确认
✅ 交易成功后显示哈希
✅ 表单重置
✅ 切换到浏览标签
```

### 场景 2：质押金额不足
```
输入：
- 质押：5 TRX（< 10 TRX）

预期：
❌ 提示"质押金额最低 10 TRX"
```

### 场景 3：余额不足
```
输入：
- 质押：1000 TRX
- 钱包余额：100 TRX

预期：
❌ 提示"TRX 余额不足"
```

### 场景 4：未连接钱包
```
操作：
- 未连接钱包时点击发布

预期：
❌ 按钮显示"请先连接钱包"
❌ 按钮禁用状态
```

## 常见问题

### Q: 质押的 TRX 能退回吗？
A: 目前的实现是将 TRX 发送到合约地址。未来版本会实现质押金管理，允许模型提供者在满足条件时提取质押金。

### Q: 发布后多久能看到模型？
A: 当前版本发布后需要手动审核。未来版本会实现自动上线机制。

### Q: 如何修改已发布的模型？
A: 当前版本不支持修改。未来版本会添加模型管理功能。

### Q: 质押金会被罚没吗？
A: 当前版本只是模拟质押。未来版本会实现基于信号准确率的自动罚没机制。

## 控制台日志

发布过程中的关键日志：
```
📤 发布模型: {
  name: "黄金价格预测模型",
  type: "gold",
  pricePerSignal: "1",
  monthlySubscription: "10",
  stakeAmount: "50",
  provider: "TXbQ8vC34TytH56r9uV2xndg5NGPP8EiWn"
}

✅ 模型发布成功，交易哈希: abc123...
```

## 下一步开发

1. **智能合约集成**
   - 实现 `publishModel()` 合约函数
   - 添加模型元数据存储
   - 实现质押金锁定和解锁

2. **信号管理**
   - 添加信号发布界面
   - 实现信号验证逻辑
   - 记录准确率统计

3. **质押金管理**
   - 实现罚没规则
   - 添加提取功能
   - 显示质押金状态

4. **模型管理**
   - 查看已发布模型
   - 编辑模型信息
   - 暂停/恢复模型
